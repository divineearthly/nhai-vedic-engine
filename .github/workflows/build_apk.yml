name: 🛡️ Build Sovereign Vision APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout Repository
      uses: actions/checkout@v4
      
    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: 📱 Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: 34
        build-tools: 34.0.0
        
    - name: 📦 Install Node Dependencies
      working-directory: NhaiApp
      run: npm install
      
    - name: 🧹 Clean CMake Cache
      run: |
        rm -rf core_kernel/build
        echo "✅ CMake cache cleaned for fresh build"
        
    - name: 🛡️ Compile Vedic C++ Core
      run: |
        cd core_kernel
        mkdir -p build && cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release
        make -j$(nproc)
        echo "✅ Vedic Core compiled"
        
    - name: 🔨 Build Android APK
      working-directory: NhaiApp/android
      run: |
        chmod +x gradlew
        ./gradlew assembleDebug --no-daemon
        
    - name: 📤 Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: SovereignVision-APK
        path: NhaiApp/android/app/build/outputs/apk/debug/app-debug.apk
        
    - name: 📊 Build Summary
      run: |
        echo "🛡️ Sovereign Vision Engine APK Built"
        echo "===================================="
        ls -lh NhaiApp/android/app/build/outputs/apk/debug/
